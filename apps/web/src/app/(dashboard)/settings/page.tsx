export default function SettingsPage() {
  return (
    <section className="panel">
      <h1 style={{ marginBottom: '0.35rem' }}>Settings</h1>
      <p className="muted" style={{ marginTop: 0, marginBottom: '0.7rem' }}>
        Account and workspace settings will expand here.
      </p>
      <div className="empty">No settings available yet.</div>
    </section>
  );
}
