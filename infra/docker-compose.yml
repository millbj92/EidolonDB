services:
  postgres:
    image: pgvector/pgvector:pg16
    container_name: eidolondb-postgres
    environment:
      POSTGRES_USER: eidolon
      POSTGRES_PASSWORD: eidolon
      POSTGRES_DB: eidolondb
    ports:
      - "5492:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U eidolon -d eidolondb"]
      interval: 5s
      timeout: 5s
      retries: 5

  server:
    build:
      context: ..
      dockerfile: infra/Dockerfile
    container_name: eidolondb-server
    network_mode: host
    environment:
      NODE_ENV: development
      PORT: 3000
      HOST: 0.0.0.0
      DATABASE_URL: postgresql://eidolon:eidolon@localhost:5492/eidolondb
      OPENAI_API_KEY: ${OPENAI_API_KEY}
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - ../apps/server/src:/app/apps/server/src
    command: pnpm dev

volumes:
  postgres_data:
